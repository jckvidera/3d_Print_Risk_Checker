How to get everything to work with Prusa and some other printers
    Ok this is a simple read me just to 
    get filmilar with the software. Basically it 
    "works" with .bgcode, which we get when directly
    slicing it with Pursa, but works a ton better
    with .gcode, which in prusa you turn off by 
    going to printers -> firmware -> and uncheck
    "Supports binariy G-code". Then when you slice it
    it allows you to directly upload the G code 
    to streamlit. 